services:
  web:
    image: node:22-bookworm
    working_dir: /app
    volumes:
      - ./web:/app
    ports:
      - "5173:5173"
    environment:
      VITE_BASE: /
    command: sh -c "npm install && npm run dev -- --host 0.0.0.0"
