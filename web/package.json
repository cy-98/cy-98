{
  "name": "cy-98-site",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "sync:notes": "node scripts/sync-cy-docs.mjs",
    "predev": "npm run sync:notes",
    "dev": "vite",
    "prebuild": "npm run sync:notes",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "three": "^0.172.0"
  },
  "devDependencies": {
    "@types/three": "^0.172.0",
    "marked": "^15.0.12",
    "sanitize-html": "^2.17.0",
    "sharp": "^0.34.2",
    "typescript": "~5.7.2",
    "vite": "^6.3.5"
  }
}
